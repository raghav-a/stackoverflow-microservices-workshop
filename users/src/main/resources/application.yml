spring:
    application:
        name: users-service

server:
    port: ${port:8095}

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    registerWithEureka: true

security:
  basic:
    enable: false

management:
  security:
    enabled: false