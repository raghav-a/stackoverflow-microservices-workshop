spring:
    application:
        name: questions-service

server:
    port: ${port:8091}
    tomcat:
      accesslog:
        enabled: true


security:
  basic:
    enable: false
  ignored: /**


management:
  security:
    enabled: false
